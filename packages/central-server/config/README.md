# Central Server Config

These allow you to configure the central server.

The config is set up using [node-config](https://github.com/node-config/node-config).

## `local.json`

```json
{
  "jwt": {
    "secret": "Generate a random string and put it here"
  }
}
```
